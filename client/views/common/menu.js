Template.menu.helpers({
    /*isActivePath : function(path){
        var currentPath = Session.get('currentPath');
        return ((currentPath) && currentPath == path)? 'active' : '';
    },
    myChannelPath : function(){
        var def = '/channel/:userId';
        var params = {userId : Meteor.userId()}
        return FlowRouter.path(def,params);
    }*/
})